name: jksoam
on: 
  push: 
    branches: 
      - main
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash
        run: |
          pwd
          ls
          top
          whoami

    
