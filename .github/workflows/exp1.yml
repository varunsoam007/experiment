name: experimet01
on: 
  push:
    branches: 
      - main
      - dev
jobs:
  install:
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: bashAction
        run: |
          pwd
          ls
          
      
  

  
